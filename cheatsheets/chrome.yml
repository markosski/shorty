---
- description: "Tab & windows"
  items:
  - shortcut:
      macos: cmd,n
      default: ctrl,n
    description: Open a new window
  - shortcut:
      macos: cmd,shift,n
      default: ctrl,shift,n 
    description: Open a new window in incognito mode
  - shortcut:
      macos: cmd,t
      default: ctrl,t
    description: Open a new tab, and jump to it
  - shortcut:
      macos: cmd,shift,t
      default: ctrl,shift, 
    description: Reopen previously closed tabs in the order they were closed
  - shortcut:
      macos: cmd,opt,rarrow
      default: ctrl,tab / ctrl,pgdn
    description: Jump to the next open tab
  - shortcut:
      macos: cmd,opt,larrow
      default: ctrl,shift,tab / ctrl,pgdn
    description: Jump to the previous open tab
  - shortcut:
      macos: cmd,1..8
      default: ctrl,1..8
    description: Jump to a specific tab
  - shortcut:
      macos: cmd,9
      default: ctrl,9
    description: Jump to last tab
  - shortcut:
      macos: NA
      default: alt,home
    description: Open your home page in the current tab
  - shortcut:
      macos: cmd,[ / cmd,larrow
      default: alt,larrow
    description: Open the previous page from browsing history in the current tab
  - shortcut:
      macos: cmd,[ / cmd,rarrow
      default: alt,rarrow
    description: Open the next page from browsing history in the current tab
  - shortcut:
      macos: cmd + w
      default: ctrl + w
    description: Close the current tab
  - shortcut:
      macos: cmd + shift + w
      default: ctrl + shift + w
    description: Close the current window
  - shortcut:
      macos: cmd + m
      default: alt + space > n
    description: Minimize the current window
  - shortcut:
      macos: NA
      default: alt + space > x
    description: Maximize the current window
  - shortcut:
      macos: cmd + w
      default: NA
    description: Hide Google Chrome
  - shortcut:
      macos: cmd + q
      default: alt + v > x
    description: Quit Google Chrome
  - shortcut:
      macos: ctrl + shift + pgup
      default: ctrl,shift,pgup
    description: Move tabs right
  - shortcut:
      macos: ctrl,shift,pgdn
      default: ctrl,shift,pgdn
    description: Move tabs left